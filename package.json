{
  "name": "trace-c",
  "version": "0.1.0",
  "private": false,
  "license": "MIT",
  "author": "Mars Arch / Matthew Faucher",
  "type": "module",
  "description": "TRACE-C — copula-calibrated temporal relational anomaly detection with rolling-conformal p-values, validated on real GB grid telemetry (NESO Open Data Licence; Supported by National Energy SO Open Data) with a frozen blind hold-out year",
  "repository": {
    "type": "git",
    "url": "https://github.com/mars-arch/trace-c.git"
  },
  "bugs": {
    "url": "https://github.com/mars-arch/trace-c/issues"
  },
  "homepage": "https://github.com/mars-arch/trace-c#readme",
  "keywords": [
    "anomaly-detection",
    "conformal-prediction",
    "copula",
    "multi-stream",
    "time-series",
    "neso",
    "trace-c"
  ],
  "engines": {
    "bun": "1.3.11"
  },
  "scripts": {
    "fetch": "bash scripts/fetch-data.sh",
    "verify:data": "python3 scripts/verify_source_data.py",
    "export:series": "bun run scripts/export-series.ts",
    "train:baselines": "${BASELINE_PYTHON:-python3} scripts/train_baselines.py",
    "eval:real": "bun run scripts/eval-real.ts",
    "eval:holdout": "bun run scripts/eval-holdout.ts",
    "eval:ablation": "bun run scripts/eval-ablation.ts",
    "eval:baselines": "bun run export:series && bun run scripts/eval-baselines.ts",
    "eval": "bun run eval:real && bun run eval:holdout && bun run eval:baselines && bun run eval:ablation",
    "baselines": "bun run train:baselines && bun run scripts/eval-baselines.ts",
    "tb:baselines": "${TENSORBOARD_PYTHON:-python3} scripts/log_tensorboard.py",
    "test:ts": "bun test",
    "test:py": "PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -p 'test_*.py'",
    "test": "bun run test:ts && bun run test:py",
    "typecheck": "tsc -p tsconfig.json",
    "verify:artifacts": "bun run scripts/verify-artifacts.ts",
    "check:core": "bun run test:ts && bun run typecheck && bun run verify:artifacts",
    "check": "bun run check:core && bun run test:py"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "5.9.3"
  }
}
